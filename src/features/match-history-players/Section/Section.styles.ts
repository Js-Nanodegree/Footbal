import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  sectionTitle: { marginLeft: 12, marginBottom: 8, marginTop: 12 },
  list: { paddingHorizontal: 4, marginBottom: 10 },
});

export default styles; 