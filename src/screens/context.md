# context.md

Purpose: Хранит все экраны, используемые в навигации приложения. Каждый экран — отдельный файл, только UI-логика, без бизнес-логики. Бизнес-логика и API — в features/hooks/services.

- TeamListScreen.tsx — список команд
- TeamDetailsScreen.tsx — детали команды 