import { colors } from '../theme/colors';

export const matchEventColorMap = {
    goal: colors.primary,
    'yellow-card': colors.yellowCard,
    'red-card': colors.redCard,
    substitution: colors.info,
    assist: colors.success,
    in: colors.success,
    out: colors.error,
    'own-goal': colors.warning,
    penalty: colors.info,
    default: colors.textSecondary,
    background: colors.card,
}; 