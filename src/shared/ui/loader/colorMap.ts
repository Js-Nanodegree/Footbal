import { colors } from '../theme/colors';

export const loaderColorMap = {
    primary: colors.primary,
    secondary: colors.secondary,
    success: colors.success,
    error: colors.error,
    info: colors.info,
    background: colors.card,
}; 